version: '3'

services:
  db:
    image: postgres
    # volumes:
    #   - /home/db:/var/lib/postgresql/data

  web:
    build: .
    volumes:
      - .:/app
    image: eeazycrm:1
    ports:
      - "5000:5000"
    depends_on:
      - db
